#!/usr/bin/env sh
set -e

# Add bun to PATH if it exists
if [ -d "$HOME/.bun/bin" ]; then
  export PATH="$HOME/.bun/bin:$PATH"
fi

echo "husky: running lint"
bun run --silent lint

echo "husky: running typecheck:all"
bun run --silent typecheck:all


